#include "FA_WAV.h"

FA_PCM
FA_ReadWavFile (const char *filename)
{
    FA_PCM pcm;

    // Initialize the pcm struct here

    retun pcm;
}

void
FA_ReadWavFile (const char *filename,
                FA_PCM      pcm)
{
    // Write the pcm struct to a file here
}

